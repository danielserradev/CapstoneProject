@model RentX.Models.ItemQueueViewModel
@using RentX.Models
@{
    ViewBag.Title = "GetItemQueue";
}

<h2>GetItemQueue</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <td>
            @Html.DisplayNameFor(model => model.RenterFirstNames)
        </td>
        <td>
            @Html.DisplayNameFor(model => model.RenterLastNames)
        </td>
    </tr>

    @foreach (string item in Model.RenterFirstNames)
    {
        <tr>
            <td>
                @Html.DisplayFor(model => model.RenterFirstNames)
            </td>

        </tr>
    }

    @for (int i = 0; i < Model.RenterFirstNames.Count; i++)
    {
<tr>
    <td>
        @Html.DisplayFor(model => model.RenterFirstNames)
    </td>
    <td>
        @Html.DisplayFor(model => model.RenterLastNames)
    </td>
</tr>
    }
</table>   
